{
  "name": "@cloudware-casper/casper-select-lit",
  "main": "casper-select-lit.js",
  "version": "0.0.35",
  "description": "A casper-select made in lit",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "scripts": {
    "start": "web-dev-server --open demo/ --node-resolve",
    "start:watch": "web-dev-server --open demo/ --node-resolve --watch"
  },
  "dependencies": {
    "@cloudware-casper/casper-icons": "^3.x",
    "@cloudware-casper/casper-virtual-scroller": "^0.x",
    "@polymer/paper-input": "^3.x",
    "@popperjs/core": "^2.x",
    "lit": "^2.x",
    "popper-max-size-modifier": "^0.2.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.18",
    "@webcomponents/webcomponentsjs": "^2.5.0"
  }
}
